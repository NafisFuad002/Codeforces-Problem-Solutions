//https://codeforces.com/contest/2114/problem/A
#include<bits/stdc++.h>
using namespace std;

#define nl cout<<"\n";
#define pf cout<<
#define sf cin>>
#define ll long long
#define py pf "YES"; nl
#define pn pf "NO"; nl

void fun()
{
    int n,x,y,z;
    sf n;
    double m;
    if((int)sqrt(n)==(double)sqrt(n))
    {
        x=sqrt(n);
        pf "0 "<<x;
    }
    else
    {
        pf "-1";
    }
    nl
}
int main()
{
  ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    sf t;
    while(t>0)
    {
       fun();
       t--;
    }

    return 0;
}
