#include<bits/stdc++.h>
using namespace std;

#define nl cout<<"\n";
#define pf cout<<
#define sf cin>>
#define ll long long

void fun()
{
    int n,a=0,b=0;
    sf n;
    string s;
    sf s;
    for(int i=0; i<s.length(); i++)
    {
        if(s[i]=='0')
        {
            a++;
        }
        else
        {
            b++;
        }
    }
    pf (a*(b+1)) + (b*(b-1));
    nl
}
int main()
{
  ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    sf t;
    while(t>0)
    {
       fun();
       t--;
    }

    return 0;
}
