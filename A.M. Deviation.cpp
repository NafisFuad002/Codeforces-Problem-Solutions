//https://codeforces.com/problemset/problem/1605/A
#include<bits/stdc++.h>
using namespace std;

#define nl cout<<"\n";
#define pf cout<<
#define sf cin>>
#define ll long long
#define py pf "YES"; nl
#define pn pf "NO"; nl

void fun()
{
    ll temp,sum=0;
    sf temp;
    sum+=temp;
    sf temp;
    sum+=temp;
    sf temp;
    sum+=temp;
    temp=(sum%3==0) ? 0:1;
    pf temp;
    nl
}
int main()
{
  ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    sf t;
    while(t>0)
    {
       fun();
       t--;
    }

    return 0;
}
