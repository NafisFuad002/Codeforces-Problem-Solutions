#include<bits/stdc++.h>
using namespace std;

#define nl cout<<"\n";
#define pf cout<<
#define sf cin>>
#define ll long long

void fun()
{
    int n,c=0;
    sf n;
    string s,s2;
    sf s;
    s2=s;
    sort(s.begin(),s.end());
    for(int i=0; i<s.length(); i++)
    {
        if(s[i]!=s2[i]) c++;
    }
    pf c;
    nl
}
int main()
{
  ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    sf t;
    while(t>0)
    {
       fun();
       t--;
    }

    return 0;
}
